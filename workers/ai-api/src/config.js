export function getConfig(env={}){const models=parseModels(env.SUPPORTED_MODELS||env.DEFAULT_MODEL||'gpt-5');return{version:env.AI_API_VERSION||'1.0.0',defaultModel:env.DEFAULT_MODEL||models[0]?.id||'gpt-5',models,origins:String(env.CORS_ALLOWED_ORIGINS||'').split(',').map(v=>v.trim()).filter(Boolean),maxBytes:number(env.MAX_REQUEST_BYTES,65536),timeoutMs:number(env.PROVIDER_TIMEOUT_MS,30000),rateLimit:number(env.RATE_LIMIT_REQUESTS,60),rateWindow:number(env.RATE_LIMIT_WINDOW_SECONDS,60),cacheTtl:number(env.CACHE_TTL_SECONDS,3600)}}
function number(value,fallback){const parsed=Number.parseInt(value,10);return Number.isFinite(parsed)&&parsed>0?parsed:fallback}
function parseModels(value){try{const parsed=JSON.parse(value);if(Array.isArray(parsed))return parsed.map(normalise).filter(Boolean)}catch{}return String(value).split(',').map(item=>normalise(item.trim())).filter(Boolean)}
function normalise(value){if(!value)return null;if(typeof value==='string')return{id:value,provider:'openai',capabilities:value.includes('embedding')?['embeddings']:['chat','responses']};if(typeof value==='object'&&value.id)return{id:String(value.id),provider:value.provider||'openai',capabilities:Array.isArray(value.capabilities)?value.capabilities:['chat','responses']};return null}