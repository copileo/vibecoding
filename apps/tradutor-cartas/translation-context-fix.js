// Legacy compatibility shim.
// Front and back translations are now independent and this module is intentionally not loaded by index.html.
// Kept temporarily because the PR validation workflow still syntax-checks this historical asset.
export {};
